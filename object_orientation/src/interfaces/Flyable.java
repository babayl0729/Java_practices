package interfaces;

//an interface is basically a contract.
// and any class that implements that inteface
//it becomes compulsory for that class to implement
//the methods that are define in that interface.
//an interface has methods that do not have a body.
//this methods are called abstract methods.
//an interface only has abstract methods

// an abstract it's an abstraction of an idea
// the general idea that something is able to fly.
public interface Flyable {
    public void fly();

}
